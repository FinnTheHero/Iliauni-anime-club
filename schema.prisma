datasource db {
    provider = "mongodb"
    url      = "mongodb+srv://Laptop-User:SptydJMTPf60U8mS@iliauni.pbn8isi.mongodb.net/Iliauni?retryWrites=true&w=majority"
}

generator client {
    provider = "prisma-client-js"
}

model User {
    id        String   @id @default(auto()) @map("_id") @db.ObjectId
    createdAt DateTime @default(now())
    email     String   @unique
    name      String?
    role      Role     @default(GUEST)
}

enum Role {
    USER
    GUEST
    ADMIN
}